# System Audit — 2026-07-12 (phase 1: seams)

Triggered by user after repeated user-discovered bugs ("I don't know how many times I have to find issues"). Phase 1 = the data-contract seams where every recent bug lived. Phase 2 (below) = systematic sweep.

## Findings

| # | Finding | Status |
|---|---------|--------|
| F1 | **Zombie watchlist rows** — both archive paths (`entered_position`, `age_out`) set `status: archived` but never reset `priority`. 87 archived rows still carried tier priorities; ~20 paraded as "entry-ready" for months (FIGS/SNDK/NVDA class), un-demotable because the demotion loop correctly skips archived rows. Root cause of the "48 entry-ready" bloat and the dead-looking stale-demotion timer (which was fine). | **FIXED** — archive paths now set `priority: "archived"`; 87-row migration applied |
| F2 | **Reactivation paths didn't restore priority** — 5 of 10 reactivation sites set `status: watching` without touching priority (masked pre-F1 because archive never changed it; exposed by F1's fix). | **FIXED** — restore-to-floor: `archived/None → watching`, never downgrades a real tier |
| F3 | **Holiday runs pollute data** — all agents ran on the July-3 market holiday (July 4 observed): 6 dated files written with stale data, 45 duplicate rows in `sector_rotation_history.json` (double-counted −4.5% in any chained calc), `market_monitor_2026-07-03.json` records 7/2's breadth under 7/3's date. No agent checks the trading calendar. | History rows **CLEANED**; dated files left (noted); **guard = top open fix** — `is_trading_day()` early-exit via Alpaca `/v2/calendar` in every scheduled agent. Needs spec + user OK |
| F4 | ~~etf_rotation.json writes rs_score: None~~ — **RETRACTED, false alarm**: assistant read `metrics.rs_score` (doesn't exist) instead of top-level `rs_score`. Feed is healthy. Lesson: schema misreads happen to the auditor too; contract docs per file would prevent both. | RETRACTED |
| F5 | **Ad-hoc IEX math vs production data** — Friday's "SMH −8.5% money out of semis" table was computed from thin local IEX bars and flattened an intra-week reversal (SMH RS 37→79, rank +19 same week). Narrative claims must come from production files (`sector_rotation_history.json`), not ad-hoc bar math. | Process rule saved to assistant memory |
| F6 | **entry-ready has no cap** — promotions are uncapped, so the tier re-bloats every run (36 active the day after a manual 48→22 cut). | OPEN — chart-review spec (cap 5 + review page) proposed 7/10, awaiting user yes |
| F7 | `_update_watchlist` used `date.today()` instead of the `today` parameter for the age-out cutoff — non-deterministic for tests/replays; one test fixture silently depended on the bug. | **FIXED** — cutoff from `today` param; fixture corrected |
| F8 | Origin of the "SMH RS 23" the user saw: **not found** — not in any snapshot this week (min 37 on 7/2) nor this session's output. If it recurs, capture a screenshot/source URL. | UNRESOLVED |

## Phase 2 plan (next sessions)

1. **Producer/consumer contract map** — for every `data/*.json`: writer, schema, readers, and a `tests/test_contracts.py` asserting the fields each reader needs exist in what the writer produces. This class (F1/F2/F4) is the #1 bug source.
2. **Silent-except census** — grep every `except Exception` + `pass/continue/return None`; classify which can hide a production break (the "0 passed, green" class from 7/3's scrape rot).
3. **Trading-calendar guard** (F3) — one shared `agents/utils/market_calendar.py`, early-exit in all scheduled entry points.
4. **Date-handling review** — every `date.today()` / `datetime.now()` in agent code vs passed-parameter dates (F7 class).
